<!doctype html>
<html lang="es-MX">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Klupp — Agencia de Marketing Data‑Driven</title>
  <meta name="description" content="Agencia de marketing enfocada en crecimiento: performance ads, funnels y automatización. Escríbenos por WhatsApp y escala tus resultados." />
  <meta name="theme-color" content="#111827" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800&display=swap" rel="stylesheet">
  <meta property="og:type" content="website" />
  <meta property="og:title" content="Klupp — Agencia de Marketing Data‑Driven" />
  <meta property="og:description" content="Performance marketing y automatización para escalar ventas. Contáctanos por WhatsApp." />
  <meta property="og:url" content="https://klupp.mx/" />
  <meta property="og:image" content="/og-image.jpg" />
  <link rel="icon" href="/favicon.ico" />
  <style>
    :root{
      --bg:#0b0f14;      /* background base */
      --panel:#111827;   /* cards/nav */
      --muted:#94a3b8;   /* slate-400 */
      --text:#e5e7eb;    /* gray-200 */
      --brand:#22c55e;   /* emerald-500 */
      --brand-700:#16a34a;
      --ring: rgba(34,197,94,.35);
      --max:1200px;
    }
    *{box-sizing:border-box}
    html,body{height:100%}
    body{
      margin:0; font-family:Inter,system-ui, -apple-system, Segoe UI, Roboto, Ubuntu, Cantarell, Noto Sans, Helvetica, Arial, "Apple Color Emoji","Segoe UI Emoji";
      color:var(--text); background:radial-gradient(1200px 600px at 70% -10%, rgba(34,197,94,.15), transparent 60%),
                         radial-gradient(800px 400px at 10% 20%, rgba(59,130,246,.12), transparent 60%),
                         var(--bg);
      line-height:1.6;
    }
    a{color:inherit; text-decoration:none}
    img{max-width:100%; display:block}

    /* layout */
    .wrap{max-width:var(--max); margin:0 auto; padding:0 20px}

    /* navbar */
    .nav{
      position:sticky; top:0; z-index:50; backdrop-filter:saturate(120%) blur(10px);
      border-bottom:1px solid rgba(148,163,184,.08);
      background:linear-gradient(180deg, rgba(17,24,39,.75), rgba(17,24,39,.55));
    }
    .nav .inner{display:flex; align-items:center; justify-content:space-between; height:64px}
    .brand{display:flex; gap:10px; align-items:center; font-weight:800; letter-spacing:.2px}
    .brand .logo{width:28px; height:28px; border-radius:8px; background:linear-gradient(135deg, #22c55e, #10b981); display:inline-block}
    .nav a.link{opacity:.9; padding:10px 14px; border-radius:10px}
    .nav a.link:hover{background:rgba(148,163,184,.08)}
    .btn{display:inline-flex; gap:10px; align-items:center; justify-content:center; padding:12px 18px; border-radius:12px; font-weight:700; background:var(--brand); color:#062b13; box-shadow:0 10px 30px var(--ring); transition:.2s}
    .btn:hover{transform:translateY(-2px); background:var(--brand-700)}

    /* hero */
    .hero{padding:96px 0 56px}
    .eyebrow{display:inline-block; padding:6px 10px; font-size:12px; text-transform:uppercase; letter-spacing:.12em; color:#a7f3d0; background:rgba(16,185,129,.08); border:1px solid rgba(16,185,129,.15); border-radius:999px}
    .hero h1{font-size:clamp(34px, 6vw, 56px); line-height:1.05; margin:16px 0 12px}
    .lead{font-size:clamp(16px, 2.2vw, 20px); color:var(--muted); max-width:780px}
    .cta-row{display:flex; gap:14px; flex-wrap:wrap; margin-top:28px}

    /* trust */
    .trust{display:grid; grid-template-columns:repeat(2,1fr); gap:18px; margin-top:40px}
    .trust .card{background:var(--panel); border:1px solid rgba(148,163,184,.08); padding:14px 18px; border-radius:14px; color:var(--muted); font-size:14px}
    @media(min-width:720px){.trust{grid-template-columns:repeat(4,1fr)}}

    /* sections */
    section{padding:72px 0; border-top:1px solid rgba(148,163,184,.08)}
    h2{font-size:clamp(26px, 3.6vw, 36px); margin:0 0 14px}
    .muted{color:var(--muted)}

    /* grid cards */
    .grid{display:grid; gap:18px}
    @media(min-width:860px){.grid.cols-3{grid-template-columns:repeat(3,1fr)}}
    .card{background:var(--panel); border:1px solid rgba(148,163,184,.08); border-radius:16px; padding:22px}

    /* KPI */
    .kpis{display:grid; gap:16px; grid-template-columns:repeat(2,1fr)}
    @media(min-width:860px){.kpis{grid-template-columns:repeat(4,1fr)}}
    .kpi{background:linear-gradient(180deg, rgba(34,197,94,.08), rgba(34,197,94,.02)); border:1px solid rgba(34,197,94,.18); border-radius:16px; padding:22px}
    .kpi .num{font-weight:800; font-size:28px}
    .kpi .lbl{color:var(--muted); font-size:14px}

    /* steps */
    .steps{display:grid; gap:14px}
    @media(min-width:860px){.steps{grid-template-columns:repeat(4,1fr)}}
    .step{position:relative; padding:22px; border-radius:16px; background:var(--panel); border:1px solid rgba(148,163,184,.08)}
    .step .n{display:inline-grid; place-items:center; width:28px; height:28px; border-radius:8px; background:#064e3b; color:#a7f3d0; font-weight:700; margin-bottom:10px}

    /* footer */
    footer{padding:48px 0; color:var(--muted)}

    /* WhatsApp FAB */
    .fab{position:fixed; right:18px; bottom:18px; width:56px; height:56px; border-radius:50%;
         display:flex; align-items:center; justify-content:center; background:#25D366; color:#053816;
         font-size:26px; box-shadow:0 14px 40px rgba(0,0,0,.35); z-index:9999; transition:.2s}
    .fab:hover{transform:translateY(-2px)}

    /* small helper */
    .btn.outline{background:transparent; color:var(--text); border:1px solid rgba(148,163,184,.25)}
    .btn.outline:hover{border-color:rgba(148,163,184,.45)}
  </style>
</head>
<body>
  <!-- NAV -->
  <nav class="nav">
    <div class="wrap inner">
      <a href="#" class="brand" aria-label="Klupp">
        <span class="logo" aria-hidden="true"></span>
        <span>Klupp</span>
      </a>
      <div style="display:flex; gap:4px; align-items:center">
        <a class="link" href="#servicios">Servicios</a>
        <a class="link" href="#resultados">Resultados</a>
        <a class="link" href="#metodologia">Metodología</a>
        <a class="btn" href="https://wa.me/52XXXXXXXXXX?text=Hola,%20quiero%20impulsar%20mi%20negocio%20con%20Klupp" target="_blank" rel="noopener" aria-label="Hablar por WhatsApp">WhatsApp</a>
      </div>
    </div>
  </nav>

  <!-- HERO -->
  <header class="hero">
    <div class="wrap">
      <span class="eyebrow">Agencia de performance y automatización</span>
      <h1>Marketing <span style="color:#86efac">data‑driven</span> que convierte en ventas reales</h1>
      <p class="lead">Gestionamos campañas, funnels y automatización de mensajes para que tu negocio crezca. Pagas por resultados, no promesas.</p>
      <div class="cta-row">
        <a class="btn" href="https://wa.me/52XXXXXXXXXX?text=Hola,%20vengo%20de%20klupp.mx%20y%20quiero%20empezar" target="_blank" rel="noopener">Hablar por WhatsApp</a>
        <a class="btn outline" href="#resultados">Ver resultados</a>
      </div>
      <div class="trust" aria-label="Marcas y pruebas sociales">
        <div class="card">✅ Meta Business Partner</div>
        <div class="card">📈 > 4.2x ROAS promedio trimestral*</div>
        <div class="card">⚡ Integraciones: Shopify, WhatsApp, Make</div>
        <div class="card">🔒 CAPI / Pixel / UTM tracking</div>
      </div>
    </div>
  </header>

  <!-- SERVICIOS -->
  <section id="servicios">
    <div class="wrap">
      <h2>¿Qué hacemos?</h2>
      <p class="muted">Ejecución extremo a extremo para escalar ventas: tráfico, conversión y retención.</p>
      <div class="grid cols-3" style="margin-top:18px">
        <article class="card">
          <h3>Paid Social & Search</h3>
          <p class="muted">Meta, TikTok y Google Ads con estructura por objetivos, testing por hipótesis y presupuesto dinámico.</p>
        </article>
        <article class="card">
          <h3>Funnels & CRO</h3>
          <p class="muted">Páginas de alta conversión, análisis de eventos, pruebas A/B y mejora continua de tasa de conversión.</p>
        </article>
        <article class="card">
          <h3>Automatización</h3>
          <p class="muted">WhatsApp/Messenger, chatbots y flujos de recuperación. Integración con Shopify y CRM.</p>
        </article>
      </div>
    </div>
  </section>

  <!-- RESULTADOS -->
  <section id="resultados">
    <div class="wrap">
      <h2>Resultados</h2>
      <p class="muted">Métricas clave de los últimos periodos en cuentas administradas.</p>
      <div class="kpis" style="margin-top:18px">
        <div class="kpi"><div class="num">4.2x</div><div class="lbl">ROAS promedio*</div></div>
        <div class="kpi"><div class="num">‑28%</div><div class="lbl">Costo por adquisición</div></div>
        <div class="kpi"><div class="num">+63%</div><div class="lbl">Tasa de conversión</div></div>
        <div class="kpi"><div class="num">+180%</div><div class="lbl">Ingresos atribuidos</div></div>
      </div>
      <p class="muted" style="margin-top:10px; font-size:12px">*Estimaciones agregadas; los resultados pueden variar según industria y etapa.</p>
    </div>
  </section>

  <!-- METODOLOGÍA -->
  <section id="metodologia">
    <div class="wrap">
      <h2>Nuestra metodología</h2>
      <div class="steps" style="margin-top:12px">
        <div class="step"><div class="n">1</div><h3>Diagnóstico</h3><p class="muted">Auditoría de cuentas, tracking y embudos. Hipótesis de crecimiento priorizadas por impacto.</p></div>
        <div class="step"><div class="n">2</div><h3>Implementación</h3><p class="muted">Estructura de campañas, creatividades, páginas y automatizaciones listas para testear.</p></div>
        <div class="step"><div class="n">3</div><h3>Experimentación</h3><p class="muted">Testing continuo (audiencias, ofertas, landings) con aprendizaje semanal.</p></div>
        <div class="step"><div class="n">4</div><h3>Escalamiento</h3><p class="muted">Asignación de presupuesto a ganadores y expansión de canales.</p></div>
      </div>
      <div style="margin-top:22px">
        <a class="btn" href="https://wa.me/52XXXXXXXXXX?text=Hola,%20quiero%20una%20auditor%C3%ADa%20gratuita" target="_blank" rel="noopener">Solicitar auditoría</a>
      </div>
    </div>
  </section>

  <!-- CONTACTO / CTA FINAL -->
  <section id="contacto">
    <div class="wrap">
      <h2>Hablemos hoy</h2>
      <p class="muted">Cuéntanos tu objetivo y te proponemos un plan claro en 24‑48h.</p>
      <div class="cta-row">
        <a class="btn" href="https://wa.me/52XXXXXXXXXX?text=Hola,%20me%20interesa%20trabajar%20con%20Klupp" target="_blank" rel="noopener">WhatsApp</a>
        <a class="btn outline" href="mailto:hola@klupp.mx">hola@klupp.mx</a>
      </div>
    </div>
  </section>

  <footer>
    <div class="wrap" style="display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap">
      <div>© <span id="y"></span> Klupp. Todos los derechos reservados.</div>
      <div><a class="muted" href="#">Aviso de privacidad</a></div>
    </div>
  </footer>

  <!-- WhatsApp FAB -->
  <a class="fab" href="https://wa.me/52XXXXXXXXXX?text=Hola,%20vengo%20de%20klupp.mx" target="_blank" aria-label="Abrir WhatsApp" rel="noopener">💬</a>

  <script>
    // año auto
    document.getElementById('y').textContent = new Date().getFullYear();
  </script>

  <!-- Schema.org for Organization -->
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "Organization",
    "name": "Klupp",
    "url": "https://klupp.mx/",
    "logo": "https://klupp.mx/logo.png",
    "sameAs": [
      "https://www.facebook.com/", "https://www.instagram.com/", "https://www.linkedin.com/"
    ],
    "contactPoint": [{
      "@type": "ContactPoint",
      "contactType": "sales",
      "telephone": "+52XXXXXXXXXX",
      "areaServed": "MX"
    }]
  }
  </script>
</body>
</html>
